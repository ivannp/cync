# Specify version format
version: "{build}"

image:
  - Visual Studio 2017

platform: x64

init:
  - ps: Write-Host (Get-Location).Path
  - ps: cd c:\projects
  - ps: Write-Host (Get-Location).Path

build: off