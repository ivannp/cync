# Specify version format
version: "{build}"

image:
  - Visual Studio 2017

platform: x64

configuration:
  - Debug

init:
  - cmd: cmake --version
#  - ps: Write-Host (Get-Location).Path
#  - ps: cd c:\projects
#  - ps: Write-Host (Get-Location).Path

build:
  project: cync-windows.sln